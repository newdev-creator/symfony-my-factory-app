<form method="post" action="{{ path('product_delete', {'product': product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('product_delete_' ~ product.id) }}">
	<button class="btn">Delete</button>
</form>
